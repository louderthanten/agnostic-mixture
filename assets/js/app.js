// Generated by CoffeeScript 1.6.3
$(function() {
  $('.md-trigger').click(function() {
    return false;
  });
  $('iframe').wrap('<figure class="video"></figure>');
  $('.content img').wrap('<figure></figure>');
  $('.video').fitVids();
  $(document).on('click', '.js-reveal', function() {
    var target;
    target = $(this).attr('href');
    $(target).toggleClass('opened');
    if ($(this).hasClass('js-sign-in-opener')) {
      if ($(target).hasClass('opened')) {
        $('#username').focus();
      }
    }
    return false;
  });
  $('.js-show-pw').click(function() {
    var type;
    type = $(this).parents('.field').find('input');
    if ($(type).attr('type') === 'text') {
      $(type).get(0).type = 'password';
      $(this).text('Show password');
    } else {
      $(type).get(0).type = 'text';
      $(this).text('Hide password');
    }
    return false;
  });
  return $('.js-pill input').change(function() {
    var check, type;
    type = $(this).parents('.js-pill');
    check = $(this).parent('label');
    if ($(type).hasClass('check-buttons')) {
      return check.toggleClass('checked');
    } else if ($(type).hasClass('radio-buttons')) {
      check.siblings('label').removeClass('checked');
      return check.addClass('checked');
    }
  });
});
